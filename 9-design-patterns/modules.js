// As your application and codebase grow, it becomes increasingly important to keep your code maintainable and separated.
// The module pattern allows you to split up your code into smaller, reusable pieces.

export function add(x, y) {
  return x + y;
}

export function multiply(x) {
  return x * 2;
}
